#!/usr/bin/python3
"""Fixed command entry point; secrets are accepted only on standard input."""
import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "lib"))
from remote_desktop import main

if __name__ == "__main__":
    main()
